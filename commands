git config --global user.email "mangalsingh@euroinfotek.com"
git config --global user.name "mangaleuroinfotek"

git init
git add [filename] || -A
git status
git commit -m "msg"
git remote add origin reponame
git push origin master
git clone  reponame
git pull origin master